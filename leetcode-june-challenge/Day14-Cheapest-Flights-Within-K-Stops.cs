using System;
using System.Collections.Generic;
using System.Text;

namespace leetcode_june_challenge
{
    class Day14_Cheapest_Flights_Within_K_Stops
    {
        //public int FindCheapestPrice(int n, int[][] flights, int src, int dst, int K)
        //{
        //    bool[] visited = new bool[n];
        //    return DFS(n, flights, src, dst, K, visited);
        //}

        //private int DFS(int n, int[][] flights, int src, int dst, int k, bool[] visited)
        //{
        //    for (int i = 0; i < flights.Length; i++) {
        //        var flight = flights[i];
        //        if (visited[flight[0]]) { 
        //            if (k == 0 && flight[0] == src && flight[1] == dst) 
        //                return flight[2];

        //            visited[flight[0]] = true;

        //        }
        //    }
        //}
    }
}
